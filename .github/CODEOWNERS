# Only the steward-owned paths are code-owned.
# Listing and pack documents are deliberately not code-owned, so the checks
# alone gate them and a validated document can merge itself without a human.
# That includes a pack owner record, because the checks let a pull request add
# one only with the first version of its pack, and any other change to one
# waits for a steward.
#
# CODEOWNERS itself lives under .github/, so that directory rule covers it.
#
# The checks are code-owned wherever they live. A pull request runs the workflow
# and the tools as they exist on its own merge ref, so a listing that could also
# edit the schema or the checker would be arriving with its own gate.

/index-status.toml @KSAModding/content-manager-stewards
/tags.toml        @KSAModding/content-manager-stewards
/.github/          @KSAModding/content-manager-stewards
/tools/            @KSAModding/content-manager-stewards
/schemas/          @KSAModding/content-manager-stewards
/site/             @KSAModding/content-manager-stewards
